<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Movie</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white font-sans">

  <!-- Header -->
  <%- include("header") %>

  <!-- Form Container -->
  <div class="flex justify-center items-center min-h-screen px-4">
    <form 
      action="/addMovieData" 
      method="post" 
      enctype="multipart/form-data" 
      class="bg-[#1f1f1f] border border-gray-700 rounded-2xl p-8 w-full max-w-md shadow-xl"
    >
      <h2 class="text-3xl font-bold mb-6 text-red-500 text-center">🎬 Add New Movie</h2>

      <!-- Poster Upload -->
      <label class="block mb-4">
        <span class="text-sm text-gray-300">Movie Poster</span>
        <input 
          type="file" 
          name="image" 
          accept="image/*"
          class="w-full mt-1 text-sm text-gray-300 bg-gray-800 file:bg-red-600 file:text-white file:py-1 file:px-3 file:border-0 file:rounded-md hover:file:bg-red-700 transition"
          required
        />
      </label>

      <!-- Movie Name -->
      <label class="block mb-4">
        <span class="text-sm text-gray-300">Movie Name</span>
        <input 
          type="text" 
          name="name" 
          placeholder="e.g. Inception" 
          class="w-full mt-1 px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-red-500"
          required
        />
      </label>

      <!-- Category -->
      <label class="block mb-4">
        <span class="text-sm text-gray-300">Category</span>
        <input 
          type="text" 
          name="catagory" 
          placeholder="e.g. Sci-Fi, Thriller" 
          class="w-full mt-1 px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-red-500"
          required
        />
      </label>

      <!-- Rating -->
      <label class="block mb-6">
        <span class="text-sm text-gray-300">Rating (out of 10)</span>
        <input 
          type="number" 
          step="0.1" 
          min="0" 
          max="10" 
          name="rate" 
          placeholder="e.g. 8.7" 
          class="w-full mt-1 px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-red-500"
          required
        />
      </label>

      <!-- Submit Button -->
      <button 
        type="submit" 
        class="w-full bg-red-600 hover:bg-red-700 transition text-white font-semibold py-2 rounded-md"
      >
        ➕ Add Movie
      </button>
    </form>
  </div>

</body>
</html>
